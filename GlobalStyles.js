import {StyleSheet} from 'react-native';

export const GlobalStyles = StyleSheet.create({
  button: {
    backgroundColor: 'white',
    alignItems: 'center',
    paddingTop: 5,
    width: 40,
    height: 40,
    borderRadius: 10,
  },
});
